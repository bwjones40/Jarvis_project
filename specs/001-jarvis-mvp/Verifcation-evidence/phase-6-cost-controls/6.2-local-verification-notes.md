# Phase 6 Local Verification Notes

Date: 2026-06-15

## Scope

This pass covers Phase 6 cost controls and token reporting from `specs/001-jarvis-mvp/plan.md`, plus the Phase 6 checks listed in `docs/jarvis-mvp-verification-guide.md` Step 7.4.

## Local Verification Result

Command run from repo root:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
python -m unittest discover -s tests
```

Observed result:

```text
Ran 47 tests in 0.218s

OK
```

Follow-up `git status --short` showed no new `__pycache__` or bytecode files from the test run.

## Evidence Mapped to Requirements

| Requirement | Local evidence | Status |
| --- | --- | --- |
| Pricing table exists for Sonnet 4.6 and Haiku 4.5 | `orchestrator/utils/token_logger.py` defines `PRICING`. | Pass |
| Cost calculator applies per-million input/output rates | `calculate_cost()` is covered by `tests/test_orchestrator.py::test_calculate_cost_uses_pricing_table`. | Pass |
| Task record includes token table with total row and estimated cost | `_build_task_record()` builds `## Token Usage`, agent rows, `**Total**`, and `**Estimated cost**`. | Pass |
| Digest includes usage and weekly rollup | `_build_digest()` writes `## Usage`, `## Weekly Cost Rollup`, `Last 7 days estimated cost`, and `Task records counted`. | Pass |
| Weekly rollup works without a database | `_build_weekly_rollup()` reads markdown task records from `jarvis/tasks`. | Pass |
| Weekly rollup correctly sums multiple task records | `tests/test_obsidian_writer.py::test_digest_weekly_rollup_sums_three_task_records` covers a 3-record rollup. | Pass |
| Orchestrator context is capped and truncation is logged | `tests/test_orchestrator.py::test_orchestrator_caps_and_logs_knowledge_context` covers 3-note and per-note token caps. | Pass |
| Research cache-hit output is capped | `run_research()` uses `research.max_tokens_per_note`; covered by `tests/test_orchestrator.py::test_research_caps_cache_hit_context_to_configured_token_budget`. | Pass |
| Research AgentRun output logs cache-hit status | `run_research()` writes `cache_hit: true/false` in each Research AgentRun output. | Pass |
| Phase 6 validation wording is not falsely blocked as PII | `tests/test_pii_guard.py::test_strict_mode_allows_phase_6_validation_terms` and `tests/test_orchestrator.py::test_phase_6_validation_task_is_not_blocked_as_pii`. | Pass |
| Digest does not count blocked clarification runs as completed | `tests/test_obsidian_writer.py::test_digest_does_not_count_clarification_runs_as_completed`. | Pass |
| Anthropic API smoke test is explicit and token-reporting capable | `tests/test_anthropic_smoke.py` covers the smoke helper and token extraction; GitHub Actions now runs `python -m orchestrator.utils.anthropic_smoke --model claude-haiku-4-5 --prompt hello`. | Pending live GitHub run |

## Operator Evidence Status

Captured screenshots:

- `6.3 Task token table generated - blocked clarification run.png`
- `6.4 Digest weekly cost rollup generated - blocked clarification run.png`

These screenshots prove the token table and weekly rollup render, but they were captured from a `needs_clarification` run before the PII false-positive fix.

For final sign-off, rerun the same Phase 6 validation task after this fix and replace or supplement those screenshots with:

- A generated task record showing `Status: completed`, the token table, total row, and estimated cost.
- A generated nightly digest showing the task under `Tasks Completed` and the weekly cost rollup.
- If the sign-off requires live behavior rather than local tests, three real task records from the same 7-day window so the rollup can count prior markdown task files.
- A GitHub Actions screenshot of `Validate Anthropic API smoke test` showing success with nonzero input/output token counts.
- A screenshot of the orchestrator AgentRun output showing `context_limits`, no more than 3 `knowledge_context` entries, and `truncated: true` for any over-limit note.
- A screenshot of the research AgentRun output showing `cache_hit: true` or `cache_hit: false`.

## Notes

- Current deterministic agents log zero token usage unless synthetic or future real LLM token usage is added.
- The Anthropic smoke test proves the repository secret can call Claude, but it does not make normal Jarvis agents LLM-backed.
- `$0.00` estimated cost is expected for deterministic local Jarvis runs.
- The evidence folder name is currently `Verifcation-evidence` in the repo; this note preserves the existing path rather than renaming user evidence folders.
